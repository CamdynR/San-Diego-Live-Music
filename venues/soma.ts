// soma.ts

import { Venue, Show, JSDOM } from '../Venue.ts';

export const soma: Venue = {
  name: 'SOMA San Diego',
  url: 'https://www.somasandiego.com/events/',
  fetchSchedule: async () => [],
};
